This requires [.Net Core](http://dotnet.github.io/) installed to run. .Net Core requires OpenSSL to be installed. This is known to run under version `1.0.0.001598` on Mac OSX.

To compile and run the soultion against the large input file and output the results:
```
./buildAndRunLargeIn.sh > A-large-practice.out
```
